import React, {useRef, useState, lazy} from 'react';
import '../styles/contactBar.css';

const Contato = ({nome, texto}) => {
    return(
        <div className="contact">
            <p>*Contato*</p>
        </div>
    );
};

export default Contato;